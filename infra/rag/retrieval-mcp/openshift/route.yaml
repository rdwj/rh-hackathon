apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: retrieval-mcp
  labels:
    app: retrieval-mcp
    app.kubernetes.io/name: retrieval-mcp
    app.kubernetes.io/part-of: griot-rag
spec:
  to:
    kind: Service
    name: retrieval-mcp
  port:
    targetPort: 8080
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
