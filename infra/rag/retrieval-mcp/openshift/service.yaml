apiVersion: v1
kind: Service
metadata:
  name: retrieval-mcp
  labels:
    app: retrieval-mcp
    app.kubernetes.io/name: retrieval-mcp
    app.kubernetes.io/part-of: griot-rag
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
  selector:
    app: retrieval-mcp
